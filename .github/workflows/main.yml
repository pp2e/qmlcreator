on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
          
    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: 6.7.*
        host: 'mac'
        target: 'ios'
        arch: 'ios'
        dir: '${{ runner.temp }}'
        install-deps: 'true'
        cache: true
      
    - name: Build
      run: |
        cmake -B ./build -GNinja
        cmake --build ./build
    - run: ls ./build
